<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sample.common.dao.mapper.TaskMapper">

	<select id="selectPageByUser" resultType="sample.common.dao.entity.Task">
		SELECT * FROM tasks
		WHERE username = #{username}
		ORDER BY id
		DESC
		LIMIT #{limit} OFFSET #{offset}
	</select>

	<select id="countTotalByUser" resultType="long">
		SELECT COUNT(*) FROM tasks WHERE username = #{username}
	</select>

	<select id="findByIdAndUser" resultType="sample.common.dao.entity.Task">
		SELECT * FROM tasks
		WHERE id = #{id} AND username = #{username}
	</select>

	<insert id="insertTaskByUser">
		INSERT INTO tasks (
		title,
		content,
		start_date,
		end_date,
		username
		) VALUES (
		#{request.title},
		#{request.content},
		#{request.startDate},
		#{request.endDate},
		#{username}
		)
	</insert>

	<update id="updateTaskByUser">
		UPDATE tasks
		SET
		title = #{request.title},
		content = #{request.content},
		start_date = #{request.startDate},
		end_date = #{request.endDate},
		updated_at = NOW()
		WHERE id = #{id} AND username = #{username}
	</update>

	<delete id="deleteTaskByUser">
		DELETE FROM tasks
		WHERE id = #{id} AND username =
		#{username}
	</delete>


	<select id="selectTasksForExportByPeriod" resultType="sample.common.dao.entity.Task">
		SELECT * FROM tasks
		WHERE username = #{username}
		AND start_date &gt;= #{startDate}
		AND end_date &lt;= #{endDate}
	</select>

	<select id="selectTaskForExportById" resultType="sample.common.dao.entity.Task">
		SELECT * FROM tasks
		WHERE username = #{username}
		AND id = #{id}
	</select>

	<!-- 旧更新用SQL -->
	<insert id="insertTask" parameterType="sample.common.dao.entity.Task" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO tasks (
		title,
		content,
		start_date,
		end_date,
		username
		) VALUES (
		#{title},
		#{content},
		#{startDate},
		#{endDate},
		#{username}
		)
	</insert>

	<update id="updateTask">
		UPDATE tasks
		SET
		title = #{title},
		content = #{content},
		start_date = #{startDate},
		end_date = #{endDate},
		updated_at = NOW()
		WHERE id = #{id} AND username = #{username}
	</update>

</mapper>