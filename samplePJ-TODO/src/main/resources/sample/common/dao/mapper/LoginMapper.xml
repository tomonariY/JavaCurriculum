<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="sample.common.dao.mapper.LoginMapper">

    <select id="findAllLogins" resultType="sample.common.dao.entity.Login">
        SELECT * FROM login
    </select>
    
    <select id="findUser" resultType="sample.common.dao.entity.Login">
		SELECT * FROM login
		WHERE username = #{username} AND password = #{password}
    </select>
    
    <select id="findByUsername" resultType="sample.common.dao.entity.Login">
		SELECT * FROM login WHERE username = #{username}
	</select>	
    
    <insert id="insertUser" parameterType="sample.common.dao.entity.Login">
		INSERT INTO login (username, password)
		VALUES (#{username}, #{password})
    </insert>
	
</mapper>